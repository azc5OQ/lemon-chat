<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".IncomingCallActivity">

    <!-- Dark overlay for better contrast -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#99000000" />

    <!-- Main Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <!-- Caller Avatar Placeholder -->
        <ImageView
            android:id="@+id/caller_avatar"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginBottom="24dp"
            android:src="@drawable/ic_person_placeholder"
            android:background="@drawable/circle_background"
            android:scaleType="centerCrop"
            android:contentDescription="@string/caller_avatar_desc" />

        <!-- Caller Name -->
        <TextView
            android:id="@+id/caller_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="John Doe"
            android:textColor="@android:color/white"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- Call Status (e.g., "Incoming Call") -->
        <TextView
            android:id="@+id/call_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Incoming Call"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:alpha="0.8" />

    </LinearLayout>

    <!-- Bottom Control Panel -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="48dp"
        android:paddingHorizontal="32dp">

        <!-- Traditional Button Container -->
        <LinearLayout
            android:id="@+id/button_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:visibility="visible">

            <!-- Decline Button -->
            <RelativeLayout
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_marginEnd="48dp"
                android:background="@drawable/decline_button_background"
                android:clickable="true"
                android:focusable="true"
                android:onClick="onDeclineCall">

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:layout_centerInParent="true"
                    android:src="@drawable/ic_call_decline"
                    android:contentDescription="@string/decline_call_desc" />

            </RelativeLayout>

            <!-- Accept Button -->
            <RelativeLayout
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:background="@drawable/accept_button_background"
                android:clickable="true"
                android:focusable="true"
                android:onClick="onAcceptCall">

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:layout_centerInParent="true"
                    android:src="@drawable/ic_call_answer"
                    android:contentDescription="@string/accept_call_desc" />

            </RelativeLayout>

        </LinearLayout>

    </RelativeLayout>

</RelativeLayout>